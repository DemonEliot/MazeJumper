using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CubeClick : MonoBehaviour {

    //This script is to be attached to all "Top" layer of each cube, so that if you click, it will automatically
    //Select the cube itself, instead of the top.

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
